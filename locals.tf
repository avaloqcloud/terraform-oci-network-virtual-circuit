locals {
  # Your 'locals.tf' code goes here.

  # locals.tf contains local variables like constants and intermediate calculations based on input variables.
  # Those calculations are put here in order to have a more concise and readable 'main.tf' file.
}
